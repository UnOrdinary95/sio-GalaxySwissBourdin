# Workflows

## Workflow API (obligatoire)

- Toute création, modification ou suppression de route backend doit inclure une mise à jour de la documentation API située dans `apps/backend/src/docs/swagger.yaml`.
- Cette mise à jour doit suivre le skill `swagger-maintenance`.
- Toute modification de route API sans mise à jour Swagger est considérée comme incomplète.
